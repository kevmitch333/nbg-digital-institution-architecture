import type { Metadata } from 'next';
import Link from 'next/link';
import { ArrowLeft,ArrowUpRight } from 'lucide-react';
import { portfolio } from '@/content/site';

export function generateStaticParams(){return portfolio.map(v=>({slug:v.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const v=portfolio.find(x=>x.slug===slug);return v?{title:`${v.name} | NBG Portfolio`,description:v.description,openGraph:{title:`${v.name} | NBG Portfolio`,description:v.description,images:[]},twitter:{card:'summary',title:`${v.name} | NBG Portfolio`,description:v.description,images:[]}}:{title:'Portfolio | NBG'}}
export default async function VenturePage({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const v=portfolio.find(x=>x.slug===slug);if(!v)return <main className="detail-page"><p>Portfolio record not found.</p><Link href="/#ventures">Return to portfolio</Link></main>;return <main className="detail-page"><header className="detail-nav"><Link href="/#ventures"><ArrowLeft size={15}/> Portfolio</Link><Link className="wordmark" href="/"><span className="mark">N</span><span>National Brand Group</span></Link></header><section className="detail-hero"><div><span className="status">{v.stage}</span><p className="maturity">{v.maturity} / {v.category}</p><h1>{v.name}</h1></div><blockquote>{v.question}</blockquote></section><section className="detail-body"><div className="detail-main"><div className="section-index">Institutional thesis</div><p className="detail-lede">{v.description}</p><h2>Architecture in practice</h2><div className="system-list">{v.systems.map((x,i)=><div key={x}><span>{String(i+1).padStart(2,'0')}</span>{x}</div>)}</div>{v.note&&<p className="detail-note">{v.note}</p>}</div><aside><div><span>Architecture components</span>{v.components.map(x=><p key={x}>{x}</p>)}</div><div><span>Evidence status</span><p>{v.evidence}</p></div><div><span>Partnership needs</span>{v.needs.map(x=><p key={x}>{x}</p>)}</div></aside></section><section className="detail-cta"><div><span className="section-index">Build with NBG</span><h2>Does this institutional problem connect to your work?</h2></div><Link className="button primary" href="/#partner">Start a conversation <ArrowUpRight size={15}/></Link></section></main>}
